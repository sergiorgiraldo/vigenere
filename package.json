{
	"name": "vigenere",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "vitest run && next dev",
		"build": "next build && next export",
		"start": "next start",
		"lint": "next lint",
		"test": "vitest run --reporter verbose",
		"cypress": "npm run dev & npx cypress run"
	},
	"dependencies": {
		"eslint": "8.29.0",
		"eslint-config-next": "13.0.6",
		"next": "13.0.6",
		"react": "18.2.0",
		"react-dom": "18.2.0",
		"vitest": "^0.25.4"
	},
	"devDependencies": {
		"cypress": "^12.3.0",
		"husky": "^8.0.3",
		"prettier": "^2.8.3",
		"pretty-quick": "^3.1.3"
	},
	"husky": {
		"hooks": {
			"pre-commit": "pretty-quick --staged"
		}
	}
}
